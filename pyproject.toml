[project]
name = "my_fuzzer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "tqdm>=4.66.0",
    "coverage>=7.13.4",
    "pandas>=3.0.1",
    "matplotlib>=3.10.0",
    "requests>=2.31.0",
    "pyparsing>=3.1.0",
    "rich>=14.0.0",
]

[tool.ruff]
line-length = 88

[dependency-groups]
dev = [
    "mypy>=1.19.1",
    "pre-commit>=4.5.1",
    "pytest>=9.0.2",
    "ruff>=0.15.2",
]
